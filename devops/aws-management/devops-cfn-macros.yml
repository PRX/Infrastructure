AWSTemplateFormatVersion: "2010-09-09"
Description: >-
  Creates all the standard, organization-wide CloudFormation macros that are
  developed internally. This stack is intended to be launched once in each
  account, which makes the macros available in all regions in those accounts.
  The Lambda functions backing the macros each only exist once globally.

# Parameters:
#   PrxUtility20210622FunctionArn:
#     Type: String

Resources:
  Placeholder:
    Type: AWS::SNS::Topic
  # PrxUtility20210622:
  #   Type: AWS::CloudFormation::Macro
  #   Properties:
  #     Description: A general-purpose, whole-template transformer macro.
  #     FunctionName: !Ref PrxUtility20210622FunctionArn
  #     # LogGroupName: String
  #     # LogRoleARN: String
  #     Name: PRX-Utility-2021-06-22
