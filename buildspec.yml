version: 0.2
env:
  variables:
    PRX_NULL: "null"
phases:
  install:
    commands:
      - 'echo "Installing CloudFormation Linter"'
      - 'pip install cfn-lint'
  build:
    commands:
      - 'cd $(ls -d */|head -n 1)'
      - 'cfn-lint --template cd/cd.yml'
      - 'echo "Done"'
      