name: The World DNS Notifier

on:
  workflow_dispatch
  # push:
  #   paths:
  #     - dns/theworld.org-hosted-zone.yml

jobs:
  email:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-region: us-east-1
          role-to-assume: arn:aws:iam::578003269847:role/DevOps-GitHubOidcFederatedRole
          role-session-name: GitHubActions-TheWorldDnsNotifier
      - name: Send email
        run: |
          echo "${{secrets.MAIL_FROM}}"

      # - name: Send mail
      #   uses: dawidd6/action-send-mail@v3.5.0
      #   with:
      #     # Required mail server address:
      #     server_address: email-smtp.us-east-1.amazonaws.com
      #     # Required mail server port:
      #     server_port: 465
      #     # Optional (recommended): mail server username:
      #     username: ${{secrets.MAIL_USERNAME}}
      #     # Optional (recommended) mail server password:
      #     password: ${{secrets.MAIL_PASSWORD}}
      #     # Required mail subject:
      #     subject: "[DNS Change] theworld.org"
      #     # Required recipients' addresses:
      #     to: ${{secrets.MAIL_TO_THEWORLD_DNS}}
      #     # Required sender full name (address can be skipped):
      #     from: ${{secrets.MAIL_FROM}}
      #     # Optional plain body:
      #     body: |-
      #       A change was made to the DNS zone template for theworld.org.

      #       Details: https://github.com/PRX/Infrastructure/commits/master/dns/theworld.org-hosted-zone.yml

# aws ses send-mail --from ${{secrets.MAIL_FROM}}
