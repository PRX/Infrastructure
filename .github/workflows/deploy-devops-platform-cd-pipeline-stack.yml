name: Deploy platform CD pipeline CloudFormation stack

on:
  push:
    paths:
      - cd/template.yml

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v2
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-region: us-east-2
          role-to-assume: arn:aws:iam::561178107736:role/DevOps-GitHubOidcFederatedRole
          role-session-name: GitHubActions-DeployPlatformCdPipelineStack
      # - name: Deploy CloudFormation stacks
      #   working-directory: cd
      #   run: |
      #     # TODO Needs to SAM deploy
      #     # TODO template too big to deploy
      #     # aws cloudformation deploy --region us-east-1 --stack-name infrastructure-cd --template-file template.yml --capabilities CAPABILITY_NAMED_IAM CAPABILITY_AUTO_EXPAND --no-fail-on-empty-changeset
      #     # aws cloudformation deploy --region us-west-2 --stack-name infrastructure-cd --template-file template.yml --capabilities CAPABILITY_NAMED_IAM CAPABILITY_AUTO_EXPAND --no-fail-on-empty-changeset
