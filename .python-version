# When this version changes, the Dockerfile base image should be updated to
# match, and for local development run:
# pyenv install --skip-existing $(cat .python-version | sed 's/[a-z_\-]//g') && pyenv virtualenv $(cat .python-version | sed 's/[a-z_\-]//g') $(cat .python-version)
# pip install --upgrade pip
# pip install -r requirements.txt
venv-infrastructure-3.10.5
